function talkid(obj) {
    "use strict";
    if (obj.id === 'bratz-1') {
        const myElement = document.getElementById('cloe-talk');
         let myElement.style.display = "block";
}  if (obj.id === 'bratz-2') {
        const myElement = document.getElementById('sasha-talk');
          let myElement.style.display = "block";       
}  if (obj.id === 'bratz-3') {
        const myElement = document.getElementById('yasmin-talk');
          let  myElement.style.display = "block";
    }
}

function hideid(obj) {
    if (obj.id === 'bratz-1') {
        const myElement = document.getElementById('cloe-talk');
           let myElement.style.display = "none";
 } if (obj.id === 'bratz-2') {
        const myElement = document.getElementById('sasha-talk');
           let myElement.style.display = "none";
 } if (obj.id === 'bratz-3') {
        const myElement = document.getElementById('yasmin-talk');
           let myElement.style.display = "none";
    }
}